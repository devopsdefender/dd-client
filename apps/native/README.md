# Native App

The native app will live in this workspace and use `dd-client-core` for pairing,
agent quote verification, Noise transport, session RPCs, and PTY streaming.

No browser shell or PWA client is planned here.

